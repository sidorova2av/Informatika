p1 = int(input('Какое число нужно умножить? ----> '))
p2 = int(input('На какое число нужно умножить? -> '))
 
#Блок присвоений
i = 0
x = 0
 
#Рассмотрим случай, когда p1 и p2 - положительные числа
if p1 >= 0 and p2 >= 0:
        while i < p2:
                x = x + p1
                i = i + 1
                
 
#Рассмотрим случай, когда p1 и p2 - отрицательные числа
if p1 < 0 and p2 < 0:
        while i < l:
                x = x + (-p2)
                i = i + 1
                
 
#Рассмотрим случай, когда p1 - число отрицательное, p2 - положительное
if p1 < 0 and p2 > 0:
        while i < p2:
                x = x + (-p1)
                i = i + 1
                
                
#Рассмотрим случай, когда p1 - число положительное, p2 - отрицательное
if p1 > 0 and p2 < 0:
        while i < -p2:
                x = x + (-p1)
                i= i + 1
print('Ответ: ', x, '.', sep = '')
